{
  "name": "@dash2zero/design-tokens",
  "version": "0.1.0-m2s1",
  "private": true,
  "description": "dash2zero design tokens — color, typography, spacing, components, motion. Source: docs/brand/THEME_DECISIONS.md",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
