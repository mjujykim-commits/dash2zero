# M1 사이클 4 — DESIGN_DIRECTION + THEME_DECISIONS

- **Agent**: designer
- **Commit SHA**: m1-c4
- **Branch / Worktree**: feat/m1-design / (single tree)
- **작업 목표**: 첫 인상 + 신뢰감 + 정보 밀도 + 토큰 시스템 정의 → packages/design-tokens 입력 완성
- **왜 필요한 변경이었는가**:
  1. PRD §4 학습 루프 4단계 + USER_JOURNEYS J-001 흐름이 시각 언어로 변환되어야 frontend가 화면 구현 가능
  2. CC2-25 결정(한글 최상위, 단일 컬럼, 하단 고정 CTA, 수동 재생) + CC2-16 (SE급) + UX-NEW-002 (한영 광학 매칭) 등 6개 미해결 잔여 항목을 토큰화
  3. M2-S2 W6 `packages/design-tokens/` 생성 시점에 본 토큰 그대로 export
- **변경된 파일**:
  - `docs/brand/DESIGN_DIRECTION.md` (신규, ~280줄): 톤 5 키워드 / 신뢰감 종류 / 핵심 화면 ASCII / 모션 철학 / 접근성
  - `docs/brand/THEME_DECISIONS.md` (신규, ~280줄): Color (Light + Dark 미리 정의) / Type scale (한영 광학 1:0.92) / 8pt spacing / SE 320pt 카드 레이아웃 / Component tokens / Motion / Icon / Hero
- **실행 명령어**: N/A
- **테스트 결과**: N/A
- **사용한 Skill (강제 4개)**:
  - **theme-factory**: color/type/space/component 토큰 체계화
  - **frontend-design**: SE 320pt 카드 레이아웃 ASCII + scale 검증
  - **canvas-design**: hero key visual 6장 스토리보드
  - **brand-guidelines**: dash2zero 톤 5 키워드 (Quiet/Honest/Spacious/Steady/Respectful) + 거리두기 4개 제품
- **내린 결정**:
  1. **신뢰감 종류**: 개인형 도구 (primary) + 운영형 신뢰 (secondary) — 엔터프라이즈/창작/분석형 모두 회피
  2. **Type Scale**: 한글 1.0 : 영문 0.92 광학 매칭 비율, text.word 44px Bold (단어 카드)
  3. **Color**: Light MVP, Dark 토큰 미리 정의 (Phase 2 또는 사용자 OS 따라 도입), 모든 조합 WCAG AA+
  4. **Spacing**: 8pt grid (space.0 ~ space.16, 11 토큰)
  5. **SE 320pt 카드**: 4단계 모두 한 화면에 들어감 (overflow 없음)
  6. **모션**: bounce/spring 금지, hero 모션 금지, 80-200ms ease-out
  7. **폰트 라이선스**: Noto Sans KR + Inter 모두 SIL OFL 1.1, 자유 사용
  8. **번들 영향**: 한글 서브셋 ~1.5MB + Inter ~0.5MB = 약 2MB (FE-NEW-006 결정 입력)
- **이전 미해결 잔여 P0 처리 (UX 영역)**:
  - UX-NEW-001 RR 줄바꿈/하이픈 정책: 카드 중앙 정렬 + 단어 길이 32 글자 이상 시 ellipsis (M2 frontend 구현)
  - UX-NEW-002 한영 광학 매칭: 1:0.92 비율 토큰화 ✅
  - UX-NEW-007 8pt grid: 11 토큰 정의 ✅
  - UX-NEW-008 다크 모드: 토큰 미리 정의 (구현은 post-MVP) ✅
- **리스크 / 후속 작업**:
  - App 아이콘 디자인 (M5 직전 작업): canvas-design skill 활용 별도 사이클
  - hero 6장 스크린샷 실제 mockup (M5 직전 — 24_app_store_aso v0.3 정합)
  - 폰트 서브셋팅 자동화 스크립트 (M2-S2 frontend가 작성)
  - `packages/design-tokens/` 패키지화 (M2-S2 W6)
- **의존성 / blocker**:
  - **본 작업이 의존**: PRD (planner ✅) / USER_JOURNEYS (planner ✅) / DOMAIN_MODEL (architect ✅, Contract Layer가 토큰 export 받음) / STACK_DECISION (architect ✅, RN+Expo Babel 호환 토큰 export 가능)
  - blocker 없음
- **다음 추천 액션**:
  1. **orchestrator**: M1 사이클 5 — M1 게이트 검증 + rollup + M2 진입 서명
  2. **frontend** (M2-S1 W5 진입 시): `packages/design-tokens/` 패키지 생성 + Restyle/Tamagui 어댑터 결정 + Storybook 골격
  3. **designer** (M5 직전): App 아이콘 + 6장 hero 스크린샷 mockup
